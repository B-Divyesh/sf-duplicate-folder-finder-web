# Compare two folder trees and find duplicate folders — in the browser, no install

Live: https://duplicate-folder-finder-web.sociobot.in — built by the Param Factory (`pwa-offline`).

See `.factory/brief.json` for the researched problem this solves and `.factory/design.md` for the visual system.

## Develop

```
npm install
npm run dev
npm test
npm run build   # -> dist/
```
