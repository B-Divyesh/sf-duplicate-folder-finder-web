{
  "name": "duplicate-folder-finder-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build && node scripts/build-sw.mjs && node scripts/verify-deploy-contract.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "test": "vitest run",
    "test:claims": "npm run build && playwright test tests/e2e/claims.spec.ts --project=chromium",
    "test:claims:contract": "node scripts/run-claim-contract.mjs",
    "test:e2e": "playwright test",
    "check": "npm test && npm run build && npm run test:claims:contract && npm run test:e2e"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@axe-core/playwright": "4.10.2",
    "@playwright/test": "1.58.2",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  },
  "overrides": {
    "playwright-core": "1.58.2"
  }
}
